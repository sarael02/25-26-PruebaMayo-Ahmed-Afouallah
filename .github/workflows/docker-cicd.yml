name: Docker CI/CD

on:
  push:
    branches: [ main ]
    paths:
      - src/**
  pull_request:
    branches: [ main ]
    paths:
      - src/**

jobs:
  build-and-deploy:
    name: Build, Push and Deploy to AWS
    runs-on: ubuntu-latest
    
    steps:
      # 1. Checkout code
      - uses: actions/checkout@v4
      
      # 2. Login to DockerHub
      - name: Login to DockerHub
        uses: docker/login-action@v3 
        with:
          username: ${{ secrets.DHUB_USER }}
          password: ${{ secrets.DHUB_TOKEN }}
      
      # 3. Build and push Docker image
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true 
          tags: |
            ${{ secrets.DHUB_USER }}/${{ secrets.DOCKER_APP_NAME }}:latest
            ${{ secrets.DHUB_USER }}/${{ secrets.DOCKER_APP_NAME }}:${{ github.sha }}
      
      # 4. Copy docker-compose.yml via SCP
      - name: Copy docker-compose to AWS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_IP }}
          username: ${{ secrets.EC2_USER }}
          port: 22
          key: ${{ secrets.EC2_KEY }}
          source: "docker-compose.yml"
          target: "/home/${{ secrets.EC2_USER }}/"
          strip_components: 0
      
      # 5. Deploy Docker services on AWS
      - name: Deploy to AWS EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_IP }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_KEY }}
          port: 22
          script: |
            # Login to DockerHub on EC2
            echo "${{ secrets.DHUB_TOKEN }}" | docker login -u "${{ secrets.DHUB_USER }}" --password-stdin
            
            # Navigate to the directory
            cd /home/${{ secrets.EC2_USER }}
            
            # Pull the latest image
            docker pull ${{ secrets.DHUB_USER }}/${{ secrets.DOCKER_APP_NAME }}:latest
            
            # Stop and remove old containers
            docker compose down
            
            # Remove old images (optional: remove all to force fresh pull)
            docker rmi ${{ secrets.DHUB_USER }}/${{ secrets.DOCKER_APP_NAME }}:latest || true
            
            # Start new containers
            docker compose up -d
            
            # Clean up unused images and volumes
            docker system prune -af --volumes
            
            # Show running containers (for debugging)
            docker ps
